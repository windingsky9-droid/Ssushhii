# Contract Proposal Pack

Use these only where the scope actually matches. Keep claims factual and link to shipped work.

Portfolio: https://split-robot-mku5jt9.shipstatic.com
GitHub: https://github.com/windingsky9-droid/Ssushhii

## Junior Python / FastAPI backend

Hi — this looks like a good fit for the kind of scoped backend work I’m doing now.

I work in Python around REST APIs, provider integrations, error handling, testing, and clean handoff documentation. My recent Market Observatory project includes a Flask backend, API/provider abstraction, demo/live modes, automated tests, GitHub CI, and server-side secret handling.

For a fixed-scope FastAPI task, I’d start by confirming the endpoints and acceptance criteria, reproduce the expected behavior locally, implement the smallest reliable solution, add verification around the important paths, and document how to run it.

If you share the current repo/spec and required deliverables, I can map the work into a clear implementation plan before changing code.

## Python / Pandas ETL debugging

Hi — I’d approach this as a root-cause debugging task rather than patching the output blindly.

I’d first reproduce the aggregation mismatch, isolate whether the behavior changed with pandas/NumPy versions or data assumptions, compare intermediate frames before and after the failing step, and add a regression test around the exact totals that should remain stable.

My current Python work uses automated tests and reproducible provider/data boundaries, so I’m comfortable narrowing failures before changing implementation code.

If you can share a minimal failing dataset, the relevant aggregation function, and the old/new environment versions, I can start with a focused diagnosis.

## API / automation integration

Hi — I’m interested in this because the work is centered on keeping APIs and business systems connected reliably.

I build Python integrations, small internal tools, API clients, validation/error handling, and automation workflows. My current portfolio demonstrates API integration architecture, test coverage, GitHub CI, and a working dashboard delivered from a defined provider boundary.

For your workflow I’d document the source/target systems first, identify the highest-value automation path, build it with clear failure handling, and leave the handoff understandable instead of creating a black-box automation.

I’m comfortable starting with one small workflow so you can evaluate the delivery before expanding scope.
